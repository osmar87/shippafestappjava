app:
  upload:
    dir: ./uploads

spring:
  application:
    name: shippafestApi
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  datasource:
    url: jdbc:postgresql://72.60.136.238:5433/appshippafest
    username: gomes
    password: gomes123

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false

  sql:
    init:
      mode: never

  flyway:
    enabled: false